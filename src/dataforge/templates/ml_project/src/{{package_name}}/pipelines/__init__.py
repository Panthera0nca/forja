"""Pipelines ML ejecutables.

training.py  — carga datos, entrena, evalúa y guarda el modelo.
inference.py — carga el modelo, predice sobre datos nuevos y persiste resultados.

Ejecutar:
    python -m {{ package_name }}.pipelines.training
    python -m {{ package_name }}.pipelines.inference
"""
