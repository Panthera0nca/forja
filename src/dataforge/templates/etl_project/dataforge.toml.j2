# Manifest del proyecto DataForge. Lo usa el CLI `dfg` para detectar y operar.
[project]
name = "{{ project_name }}"
package = "{{ package_name }}"
type = "etl"
version = "0.1.0"
sources = []
pipelines = []
